#include <iostream>
using namespace std;

int main() {
  int n;

  cout << "Ingresa el tamano de tu array: ";
  cin >> n;
  int array[100];

  for (int i = 0; i < n; i++) {
    cout << "Ingresar el valor para " << i << endl;
    cin >> [i];
  }

  for (int i = 0; i < n; i++) {
    cout << array[i];
  }

  return 0;
}